# AWS (present now)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
S3_BUCKET=medrag-hotswap
ADAPTERS_PREFIX=adapters/
DOCS_RAW_PREFIX=docs/raw/
DOCS_PARSED_PREFIX=docs/parsed/

# Runpod (only API key now; endpoint later in Phase 2/3)
RUNPOD_API_KEY=
RUNPOD_ENDPOINT=  # (leave blank for now)

# Postgres / pgvector (present now)
DATABASE_URL=
# or split fields:
PGHOST=
PGDATABASE=
PGUSER=
PGPASSWORD=
PGPORT=5432
PGVECTOR_TABLE=publications
PGVECTOR_DIM=1024

# Redis (present now)
REDIS_URL=

# Models (present now)
BASE_MODEL=meta-llama/Meta-Llama-3.1-8B-Instruct
EMBED_MODEL=BAAI/bge-large-en-v1.5

# Controller config (present now)
WARM_TTL_SEC=1800
ALLOWED_ORIGINS=  # e.g., https://your-frontend-domain
NEXT_PUBLIC_API_URL=  # set when controller is deployed
